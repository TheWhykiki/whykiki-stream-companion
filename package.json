{
  "name": "whykiki-stream-companion",
  "version": "0.2.0",
  "description": "Produktions- und Archivierungssuite für Livestreams: Kommentare zeitsynchron als MOGRT-Overlays nach Premiere Pro",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "wsc": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "npm run build && node --test dist/tests/",
    "validate:examples": "node tests/validate_examples.js",
    "cli": "node dist/cli/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
